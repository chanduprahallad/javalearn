package actions.demo.arrays;

public class IfStatement {
    public static void main(String[] args) {
        //if is known as conditional statement.
        //the argument we pass as part of if must always be boolean expression.
        //boolean expression will always result in either true or false.
        //is 2 > 3? no
        if("tej".contains("ntr")){
            System.out.println("Print I am NTR");
        } else {
            System.out.println("Print I am Tej");
        }
    }
}
